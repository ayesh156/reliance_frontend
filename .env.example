# ===================================================
# Reliance Frontend — Environment
# (Retail & Wholesale POS + E-Commerce)
# ===================================================
# Copy to .env and update for your environment.

# ─── API URL ──────────────────────────────────────
# Local development:
VITE_API_URL=http://localhost:5000/api

# Production (same server — nginx proxies /api to backend):
# VITE_API_URL=/api/v1
